# PROGRESS — N8E Collect

One line per milestone: date · milestone · status · deviations.

- 2026-08-19 · **0.1 Repo + toolchain** · DONE · Next.js 16.3 (plan says 15+), TS strict, Tailwind 4, ESLint/Prettier, Vitest, Playwright, husky+lint-staged, docker compose (postgres + app dev profile), tokens.css stub, docs in place. Verified: unit + e2e green, lint/typecheck clean, dev server serves. NOT verified here: `docker compose` postgres boot (no Docker daemon in the build sandbox — re-run on the dev machine).
- 2026-08-19 · **0.2 DB layer** · DONE · Drizzle schema for all §3.1 tables + api_clients/idempotency_keys (API spec §5.4), immutability trigger + response_items expression unique index (custom migration), seed script (admin from env + demo survey). Verified: migrate clean on fresh DB, trigger rejects UPDATE/DELETE (unit-tested vs real Postgres 16.13), seed idempotent, typecheck/lint clean.
- 2026-08-19 · **0.3 Auth + API pipeline** · DONE · defineRoute pipeline (requestId/ULID, rate limit, authN/authZ scopes, zod validation, envelope, origin check), error registry, session+respondent identity planes; argon2id login → TOTP (otplib v13) → signed httpOnly SameSite=Lax 12h cookie; lockout 5 fails/15 min derived from append-only audit log (restart-safe); /admin middleware gate; login/enrol UI. Verified: 15 unit tests (auth service, lockout, ratelimit, TOTP) vs real Postgres; Playwright: happy path+enrol+logout, wrong-TOTP, lockout — all green; typecheck/lint clean. Deviation: auth rate class 30/min (was 10/min) — account lockout is the real guard; avoids e2e flake.
- 2026-08-19 · **0.4 Skeleton deploy** · DONE (partial verify) · Standalone Dockerfile (dev/runner/migrate targets), prod compose with Caddy (auto-TLS, security headers), GH Actions CI (typecheck/lint/format/licenses/unit/e2e vs Postgres service) + tag-deploy workflow (GHCR arm64 + SSH), server deploy script with health gate + rollback-by-tag, /api/core/v1/health (DB check), license allowlist gate (two-tier, see DECISIONS). Verified: standalone build boots and health returns ok envelope; license gate green; CI workflow runs on push. NOT verified here: real AWS deploy + rollback drill (needs the box + secrets DEPLOY_HOST/USER/SSH_KEY).
- 2026-08-19 · **1.1 Schema package** · DONE · Full Zod InstrumentDefinition (12 question types, rules, scoring, presentation, schemaVersion=1), publish invariants (unique keys, resolving refs, forward-only skips, show/hide cycle detection, choice/likert shape, scoring refs, key-rename detection vs previous version), 5 type presets, human-readable diff. Verified: 12 unit tests green incl. key-rename detection; typecheck clean.
- 2026-08-19 · **1.2 Logic + scoring engine** · DONE · Pure isomorphic engine: ordered rule evaluation, show-target default-hidden, last-wins show/hide, halt on disqualify/end, forward skips → unreachable pages, fixpoint hidden-answer stripping, completion-time scoring with bands; 39 golden vectors (every comparator, ordering, stripping cascades, disqualify/end, skips, scoring incl. negative points). Verified: 40 tests green; mutation spot-check (gt→gte) made a vector fail, then reverted.
- 2026-08-19 · **1.3 Instrument CRUD API + versioning** · DONE · instruments service (create-from-preset, draft update w/ slug lock after publish, publish with invariants + immutable snapshot + confirm-with-diff over collected responses, duplicate, archive, hard-delete only at zero responses, version history w/ counts); routes via defineRoute only; OpenAPI generated from route Zod schemas (pnpm api:openapi, 13 ops) + CI drift gate; generic contract test (scope enforcement + envelope on every route). Verified: 124 unit tests green ×3 runs (isolated per-suite DBs), typecheck/lint clean.
